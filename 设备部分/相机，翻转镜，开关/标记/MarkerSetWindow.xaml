<Window
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:Controls="clr-namespace:Controls;assembly=Controls" xmlns:基本控件="clr-namespace:ODMR_Lab.基本控件" x:Class="ODMR_Lab.设备部分.相机_翻转镜.MarkerSetWindow"
        mc:Ignorable="d"
        Title="图像处理参数" Height="385" Width="408"  WindowChrome.IsHitTestVisibleInChrome="False" BorderThickness="2" ResizeMode="NoResize" WindowStyle="None" Background="#FF1F1F1F" BorderBrush="#FF0075DA">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="30"/>
            <RowDefinition/>
            <RowDefinition Height="40"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="60"/>
        </Grid.ColumnDefinitions>
        <Label Content="    图像处理参数" Name="title" Grid.Row="0" IsHitTestVisible="False" Grid.ColumnSpan="1" FontWeight="ExtraBold" Background="#FF383838" Foreground="#FFFAFAFA"/>
        <Label Name="labletemplate" Grid.Row="0" IsHitTestVisible="False" HorizontalContentAlignment="Center" VerticalContentAlignment="Center" Grid.ColumnSpan="1" FontWeight="ExtraBold" Background="Transparent"  Visibility="Collapsed" Foreground="#FFFAFAFA"/>
        <Controls:DecoratedButton Name="CloseBtn" IconMargin="7" TextAreaRatio="0" Grid.Row="0" Grid.Column="3" FontWeight="Thin" FontSize="20" Background="#FF383838" Foreground="LightGray" MoveInColor="#FF424242" PressedColor="#FF39393A" MoveInForeground="LightGray" PressedForeground="LightGray" IconSource="/图片资源/close.png"/>
        <TextBox Name="BoxTemplate" Background="{x:Null}" BorderThickness="0" BorderBrush="Transparent" Foreground="White" CaretBrush="White" HorizontalContentAlignment="Center" VerticalContentAlignment="Center" HorizontalAlignment="Center" VerticalAlignment="Center" Visibility="Collapsed"/>
        <Controls:AutoScrollViewer Grid.Row="1" Margin="10" Name="scr" Grid.ColumnSpan="2">
            <Controls:AutoScrollViewer.Content>
                <StackPanel Name="ParamPanel" Orientation="Vertical" Width="{Binding ElementName=scr, Path=ActualWidth}">
                </StackPanel>
            </Controls:AutoScrollViewer.Content>
        </Controls:AutoScrollViewer>
        <Controls:DecoratedButton Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="2" Click="Apply" Margin="5" FontWeight="Thin" FontSize="14" Background="#FF383838" Foreground="LightGray" MoveInColor="#FF424242" PressedColor="#FF39393A" MoveInForeground="LightGray" PressedForeground="LightGray" Text="应用"/>
    </Grid>
</Window>
